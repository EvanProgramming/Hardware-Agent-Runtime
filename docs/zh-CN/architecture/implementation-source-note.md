# 实现来源说明

实现任务书曾引用 `docs/architecture/`、`docs/architecture/adr/`、`examples/drivers/` 与 `examples/experiments/`。开始实现时，已批准的架构实际位于根 `docs/` 文件和 `schemas/har-v1.schema.json`，部分引用目录不存在。

这是文档存放位置不一致，不是架构矛盾。根目录英文架构仍是规范来源；本目录只用于实现说明及后续 ADR，不会替代或修改已批准的边界、数据契约或决策。
